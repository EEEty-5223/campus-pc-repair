# 校园电脑维修预约小程序：第一版方案

## 1. 目标与边界

第一版只回答一个问题：社团能否用一套清晰的流程演示“学生提交维修预约，社团接单并推进状态”。因此选择最小可运行方案，不在第一版引入注册登录、付费、配件库存、复杂排班或独立管理网站。

成功标准：新成员按 README 导入项目后，可在微信开发者工具中完成一次从提交到完成的全流程，不需要申请云环境或安装依赖。

### 当前实现进度（V0.3.0）

- 已实现预约新增、待确认阶段修改和取消。
- 已实现重复时段提醒、个人预约分类筛选。
- 已实现受约束的状态流转与完整处理时间线。
- 已实现社团后台状态筛选、简单统计和一键演示数据。
- 已通过 JSON、JavaScript、页面文件、事件绑定和数据层自动测试。
- 已增加页面级模拟测试，覆盖预约提交、查看、修改、重复提醒、后台状态推进、取消和列表筛选的完整业务链路。
- 已安装微信开发者工具 `2.02.2608070`，完成真实 IDE 导入和编译，首页已在模拟器中实际渲染。
- 开发者工具首次拉起模拟器时出现过一次超时/握手警告，随后页面正常显示，日志未发现项目代码编译错误。
- 完整业务链路已由页面级模拟测试验证；真实鼠标交互和真机体验仍待人工验收，不能把自动测试与首页渲染等同于最终用户验收。

## 2. 用户与核心流程

### 学生端

1. 首页了解服务范围和注意事项。
2. 填写联系人、设备、故障、校内地点和期望时间。
3. 提交后在“我的预约”查看状态。
4. 待确认时可以修改或取消。

### 社团端

1. 从首页进入“社团成员演示后台”。
2. 查看预约的联系方式、故障摘要和时间地点。
3. 按 `待确认 → 已接单 → 维修中 → 已完成` 推进状态，也可以取消。
4. 通过筛选和统计快速查看待处理、处理中与已完成预约。

## 3. 技术方案

| 项目 | 第一版选择 | 原因 |
|---|---|---|
| 客户端 | 原生微信小程序（WXML、WXSS、JavaScript） | 无构建依赖，开发者工具直接编译 |
| UI | 自有 WXSS，参考微信原生视觉 | 避免第一版安装组件库，体积和故障面更小 |
| 数据 | `wx.setStorageSync` / `wx.getStorageSync` | 不需要服务器即可演示持久化流程 |
| 后台 | 小程序内演示页面 | 同一设备即可展示完整状态流转 |
| 身份 | 暂无 | 正式版必须改为云端鉴权 |

微信官方文档说明，原生小程序通过 `app.json` 配置页面与窗口，每个页面由 JS、JSON、WXML、WXSS 组成；本地缓存单个 key 上限 1 MB、总计 10 MB，足够第一版少量文本预约记录，但不适合长期生产数据。参考：[小程序全局配置](https://developers.weixin.qq.com/miniprogram/dev/reference/configuration/app.html)、[wx.setStorageSync](https://developers.weixin.qq.com/miniprogram/dev/api/storage/wx.setStorageSync.html)。

## 4. 数据结构

```js
{
  id: 'WX1234567890',
  name: '联系人姓名',
  phone: '11 位手机号',
  location: '校内联系地点',
  deviceType: '笔记本电脑',
  faultType: '系统与软件',
  description: '故障现象描述',
  appointmentDate: '2026-09-18',
  timeSlot: '14:00-16:00',
  status: 'pending',
  createdAt: 1780000000000,
  createdAtText: '2026-09-17 14:00',
  updatedAt: 1780000000000
}
```

状态值固定为：

- `pending`：待确认
- `accepted`：已接单
- `repairing`：维修中
- `completed`：已完成
- `cancelled`：已取消

所有数据读写集中在 `utils/appointments.js`。这是为了第二版迁移云端时，不让页面里散落数据库操作。

状态转换同样在该数据层校验：待确认只能接单或取消，已接单只能进入维修中或取消，维修中只能完成或取消，结束状态不能再次修改。页面按钮不是安全边界，数据层仍会拒绝非法跳转。每次合法变化都会追加到 `history`，详情页按时间显示处理轨迹。

## 5. 页面清单

| 页面 | 作用 | 第一版关键点 |
|---|---|---|
| 首页 | 服务介绍、流程和入口 | 强调备份数据与本机演示限制 |
| 预约维修 | 收集预约信息 | 必填校验、手机号格式、隐私同意 |
| 我的预约 | 展示当前设备预约 | 重新进入页面自动刷新状态 |
| 预约详情 | 完整信息、修改与取消 | 只有待确认预约能由用户修改或取消 |
| 社团演示后台 | 更新维修进度 | 状态筛选、简单统计；明示没有鉴权 |
| 信息使用说明 | 说明数据用途 | 正式发布前须替换为真实内容 |

## 6. GitHub 参考与取舍

- [Tencent/weui-wxss](https://github.com/Tencent/weui-wxss)：参考微信官方设计团队的基础控件与视觉习惯；第一版未复制或引入其代码，保持零依赖。
- [jiwenxing/miniprogram-demo](https://github.com/jiwenxing/miniprogram-demo)：参考原生小程序的根目录、页面四文件和直接导入开发者工具的组织方式。
- [Zeovv/Ordering-mini-program](https://github.com/Zeovv/Ordering-mini-program)：参考“用户订单 + 管理端 + 明确状态流转”的业务拆分。它使用云开发，本项目第一版先用本地数据，第二版再迁移。
- [shuaibixia/JztBorrowSystem](https://github.com/shuaibixia/JztBorrowSystem)：这是更接近学生社团场景的预约、审批与管理员项目，可作为后续云函数、角色权限、通知和管理统计的架构参考。
- [Ei-Ayw/rentease-miniprogram](https://github.com/Ei-Ayw/rentease-miniprogram)：参考预约页面与后端 API 分层，但其独立后端、实时消息和地图能力超出第一版范围。

没有直接复制上述仓库的业务代码，避免许可证、历史依赖和无关功能一起进入项目。

## 7. 正式化路线

### V1.1：多人可用

- 开通微信云开发，建立 `appointments`、`users` 两个集合。
- 云函数负责创建预约、查询本人预约和管理员更新状态。
- 通过微信上下文获取 openid，客户端不得上传或伪造用户身份。
- 管理员权限保存在云数据库，由云函数每次校验。
- 为状态更新增加允许的转换规则，禁止客户端任意改状态。

### V1.2：社团日常使用

- 增加值班时段与满额控制。
- 故障图片上传前先做隐私提示与大小限制。
- 增加预约备注、维修结果、费用/配件“仅记录不收款”。
- 增加订阅消息提醒，但必须由用户主动授权。

### V2：运营与质量

- 独立管理员工作台、成员分工、维修评价与统计。
- 数据导出、保留期限、删除申请与审计记录。
- 如果产生收费，再单独设计定价、退款、主体资质与支付合规；不要在当前原型上直接加收款按钮。

## 8. 隐私与发布门槛

预约会处理姓名、手机号和地点。微信生态要求涉及个人信息时配置用户隐私保护指引，并按实际调用的隐私接口声明用途。正式上线前至少完成：

1. 明确小程序主体和社团负责人。
2. 只收集完成预约真正需要的信息。
3. 写明保存期限、查看/更正/删除方式和联系渠道。
4. 云端数据库设置最小权限，管理员操作必须服务端鉴权。
5. 删除演示后台的公开入口，改为真实管理员判断。
6. 用体验版完成真机和隐私弹窗验证，再提交审核。

隐私规则可参考[腾讯云整理的微信小程序隐私保护指引适配说明](https://cloud.tencent.cn/document/product/1301/97930)，最终以微信公众平台提交审核时的最新要求为准。

## 9. 第一版交付结果

- 可运行的原生小程序源码
- 无第三方依赖、无账号密钥、无远程服务
- 用户端和社团端完整演示闭环
- 运行、验收和下一版迁移说明

当前版本已经达到“可导入、可编译、可展示首页”的第一版目标，适合社团内部评审和需求确认。预约全流程仍应按 README 清单做一次模拟器或真机人工验收；在接入云端鉴权、真实隐私指引和多人数据同步之前，不适合直接提交为正式线上服务。
